name: Frontend CI

on:
  push:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run ESLint
        run: yarn run lint

      - name: Build Next.js app
        run: yarn run build

      - name: Run TypeScript checks
        run: yarn run typecheck

  release:
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Clean up stale tags
        run: |
          # Get the next version that commit-and-tag-version would create
          NEXT_VERSION=$(npx commit-and-tag-version --dry-run | grep "tagging release" | sed 's/.*tagging release //')
          if [ -n "$NEXT_VERSION" ]; then
            echo "Next version tag: $NEXT_VERSION"
            # Delete local tag if it exists
            git tag -d "$NEXT_VERSION" 2>/dev/null || true
            # Delete remote tag if it exists
            git push origin --delete "$NEXT_VERSION" 2>/dev/null || true
          fi

      - name: Run release
        run: yarn release

      - name: Push version bump and tag
        run: git push --follow-tags origin main
